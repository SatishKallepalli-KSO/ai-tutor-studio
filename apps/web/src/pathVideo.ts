/** Shared video shape for career-path libraries (Agentic, Snowflake). */

export type PathVideo = {
  id: string
  title: string
  channel: string
  duration: string
  why: string
  youtubeId?: string
  playlistId?: string
  url: string
}
